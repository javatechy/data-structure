package geeksforgeeks.linkedlist;

public class LinkedList {

    Node head; // head of list

    /* Linked list Node*/
   static class Node {
        int data;
        Node next;
        Node(int d) {
            data = d;
        }
    }
}
